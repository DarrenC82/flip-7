

test('plus 2 modifier adds 2 points to score', () => {
	fail('not implemented: +2 should add 2 points after number cards');
});

test('multiple modifiers are added together', () => {
	fail('not implemented: total bonus should be sum of all modifiers');
});

test('x2 modifier doubles the number card score', () => {
	fail('not implemented: x2 should double sum of number cards only');
});

test('x2 and flat bonuses apply in correct order', () => {
	fail('not implemented: x2 applies before +4 or similar');
});

test('modifier cards do not count toward unique number cards', () => {
	fail('not implemented: modifiers should not affect Flip 7 bonus');
});

test('modifier cards are still counted even if player stays early', () => {
	fail('not implemented: modifier must apply regardless of round length');
});

test('modifier cards are ignored if player busts', () => {
	fail('not implemented: score modifiers should not apply after bust');
});

